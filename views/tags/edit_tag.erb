<div class="row">
  <div class="large-6 columns large-centered">
    <h2 class="luckypot">Add a new tag:</h2>
    <form method="post" action="/save_tag/<%= @tag.id %>">
      Tag:
      <input type="text" name="tag[name]" value="<%= @tag.name %>">

      <button>Save Tag</button>
    </form>
  </div>
</div>